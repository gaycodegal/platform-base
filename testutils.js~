function genMap (w, h){
    const arr = new Array(w);
    for(let x = 0; x < w; ++x){
	const row = arr[x] = new Array(h);
	for(let y = 0; y < h; ++y){
	    if(y == 1 || x == 1 || y == h - 2 || x == w - 2)
		row[y] = 1;
	    else
		row[y] = 0;
	}
    }
    console.log(arr);
    return arr;
}
